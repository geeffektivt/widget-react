import { css } from '../../../../styles/stitches.config'

export const title = css({ margin: 0 })

export const description = css({ margin: 0 })

export const spinner = css({ padding: '$s200 0' })

export const logo = css({
  display: 'flex',
  height: 50,
  margin: '0 auto',
  width: 'auto',
})
